spring:
  application:
    name: cosy
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/spring_db}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Jeon95921513!}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${JWT_SECRET:1RUb2tlbkdlbmVyYXRpb25BbmRWYWxpZGF0aW9u} # 바꿔야함 아무거나 집어넣은거라
  issuer: ${JWT_ISSUER:cosy}
  access-token-expiration: 10800000 # 3 hours
  refresh-token-expiration: 604800000 # 7 days
